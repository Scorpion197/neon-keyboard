{
  "name": "neon-keyboard",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cargo-cp-artifact -nc index.node -- cargo build --message-format=json-render-diagnostics",
    "build-debug": "npm run build --",
    "build-release": "npm run build -- --release",
    "install": "npm run build-release",
    "test": "cargo test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pinely-international/chronly-keyboard-recorder.git"
  },
  "author": "Scorpion197",
  "license": "ISC",
  "devDependencies": {
    "cargo-cp-artifact": "^0.1"
  },
  "keywords": [
    "keyboard",
    "events"
  ],
  "dependencies": {
    "node-addon-api": "^7.0.0"
  }
}
